* {
  font-size: 14px;
  font-family: "{{ .font.family }}";
  min-width: 6px;
  min-height: 0px;
  border: none;
  border-radius: 0;
  box-shadow: none;
  text-shadow: none;
  padding: 0px;
}

window#waybar {
  transition-property: background-color;
  transition-duration: 0.5s;
  border-radius: 6px;
  color: {{ .colors.primary.foreground }};
  background-color: alpha({{ .colors.primary.background }}, 0.0);
}

tooltip {
  border-radius: 6px;
  padding: 4px;
  border: 1px solid {{ .colors.primary.accent }};
  background-color: {{ .colors.primary.background }};
  color: {{ .colors.primary.foreground }};
}

tooltip label {
  color: {{ .colors.primary.foreground }};
}

.modules-right {
  margin: 4px 2px;
  border-radius: 4px;
  background-color: alpha({{ .colors.primary.background }}, 0.4);
  color: {{ .colors.primary.foreground }};
}

.modules-left {
  transition-property: background-color;
  transition-duration: 0.5s;
  margin: 4px 2px;
  border-radius: 4px;
  background-color: {{ .colors.primary.background }};
  color: {{ .colors.primary.foreground }};
}

#workspaces {
  margin: 4px 2px;
  padding: 4px 2px;
  border-radius: 6px;
  font-weight: bold;
}

#workspaces button {
  transition-property: background-color;
  transition-duration: 0.5s;
  padding: 4px 2px;
  background-color: transparent;
  border-radius: 4px;
  color: {{ .colors.normal.white }};
}

#workspaces button.urgent {
  padding: 4px 2px;
  font-weight: bold;
  background-color: {{ .colors.bright.red }};
  color: {{ .colors.primary.foreground }};
}

#workspaces button.active {
  padding: 4px 2px;
  background-color: alpha({{ .colors.primary.accent }}, 1.0);
  color: {{ .colors.primary.background }};
  border-radius: 4px;
}

#cpu,
#memory,
#network,
#pulseaudio,
#battery,
#tray {
  border-radius: 4px;
  padding: 4px 2px;
  margin: 4px 2px;
  font-weight: bold;
  background-color: {{ .colors.primary.background2 }};
  border: 1px solid {{ .colors.primary.selection }};
  color: {{ .colors.primary.foreground }};
}

#clock {
  border-radius: 4px;
  padding: 4px 2px;
  margin: 4px 2px;
  font-weight: bold;
  background-color: alpha({{ .colors.primary.accent }}, 1.0);
  color: {{ .colors.primary.background }};
}


#cpu.critical,
#memory.critical {
  background-color: {{ .colors.bright.red }};
  color: {{ .colors.primary.background }};
  border: 1px solid {{ .colors.normal.red }};
}

#battery.warning {
  background-color: {{ .colors.normal.yellow }};
  border: 1px solid {{ .colors.bright.yellow }};
  color: {{ .colors.primary.background }};
}

#battery.critical {
  background-color: {{ .colors.bright.red }};
  border: 1px solid {{ .colors.normal.red }};
  color: {{ .colors.primary.background }};
}

#network.disconnected {
  background-color: {{ .colors.bright.red }};
  border: 1px solid {{ .colors.normal.red }};
  color: {{ .colors.primary.background }};
}

#pulseaudio.muted {
  background-color: {{ .colors.normal.black }};
  border: 1px solid {{ .colors.bright.black }};
  color: {{ .colors.normal.white }};
}
