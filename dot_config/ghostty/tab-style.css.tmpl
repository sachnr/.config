headerbar {
  min-height: 12px;
  padding: 2px;
  margin: 0;
}

tabbar tabbox {
  min-height: 10px;
  font-family: monospace;
}

tabbar tabbox tab {
  background: {{ .colors.ui.background }}
  border: 1px solid {{ .colors.ui.background }};
  border-radius: 0;
}

tabbar tabbox tab:selected {
  background: {{ .colors.ui.background }}
  color: {{ .colors.ui.cursor }};
}
